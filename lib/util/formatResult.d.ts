import { FormatResultParams, FormatResultWithIdParams } from '../types';
export declare const formatResult: ({ result, snapshot, format, reducer, params }: FormatResultParams) => any;
export declare const formatResultWithId: ({ result, snapshot, format, id, reducer, params }: FormatResultWithIdParams) => any;
